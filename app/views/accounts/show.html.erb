<h1>Welcome <%= current_user.first_name %> </h1>

<% @comments.each do |comment| %>
    <div>
      <%= comment.movie.title %>: <%= comment.body %>
    </div>

<% end %>

<div class="right-align">
<%= button_to 'LOG OUT', destroy_user_session_path, method: :delete%>
</div>
